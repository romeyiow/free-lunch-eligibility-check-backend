# Node Environment (development, production, test)
NODE_ENV=development

# Port the application will run on
PORT=5001

# MongoDB Atlas connection string (Get from Atlas in Phase 2)
MONGO_URI=YOUR_MONGO_URI

# JSON Web Token secret for signing tokens (Create a strong random string in Phase 5)
JWT_SECRET=YOUR_SECURE_RANDOM_STRING_FOR_JWT_HERE

# Optional: JWT Token Expiration (e.g., 30d, 1h, 7d)
# JWT_EXPIRES_IN=30d

# Optional: Email configuration for password reset (Phase 5)
# EMAIL_HOST=
# EMAIL_PORT=
# EMAIL_USER=
# EMAIL_PASS=
# EMAIL_FROM=